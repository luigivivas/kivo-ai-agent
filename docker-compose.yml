services:
  kivo-agent:
    build: .
    container_name: kivo-agent
    env_file:
      - .env
    ports:
      - "8600:8501"
    volumes:
      - kivo-index:/app/index
    restart: unless-stopped

volumes:
  kivo-index:
